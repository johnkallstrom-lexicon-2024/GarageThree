@inject ISelectListItemService<Garage> selectListItemService
@model VehicleIndexViewModel

@{
    ViewBag.Title = "Vehicles";

    var garageOptions = await selectListItemService.GetSelectListItems();
}

@* recommend adding wwwroot for consolidated style overrides *@
@section Styles {
    <style>
        td {
            vertical-align: middle;
        }
    </style>
}

<h3>@ViewBag.Title</h3>
<a asp-action="Create" class="btn btn-primary">Park</a>
<div class="d-flex flew-row mt-2">
    <form asp-action="Index" method="get">
        <div class="d-flex flex-row">
            <select asp-for="@Model.GarageId" class="form-select" asp-items="garageOptions"></select>
            <button type="submit" class="btn btn-secondary ms-2">Filter</button>
        </div>
    </form>
</div>
<table class="table table-hover table-responsive-xl">
    <thead class="thead-light">
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().Id)</th>
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().RegNumber)</th>
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().Brand)</th>
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().Model)</th>
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().RegisteredAt)</th>
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().Color)</th>
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().Type)</th>
        <th>@Html.DisplayNameFor(m => m.Vehicles.First().Garage)</th>
        <th scope="col"></th>
        <th scope="col"></th>
        <th scope="col"></th>
    </thead>
    <tbody>
        @foreach (var vehicle in Model.Vehicles)
        {
            <tr>
                <td>@vehicle.Id</td>
                <td>@vehicle.RegNumber</td>
                <td>@vehicle.Brand</td>
                <td>@vehicle.Model</td>
                <td>@vehicle.RegisteredAt</td>
                <td>@vehicle.Color</td>
                <td>@vehicle.Type.Name</td>
                <td>
                    <a asp-controller="Garages" asp-action="Details" asp-route-id="@vehicle.Garage.Id">@vehicle.Garage.Name</a>
                </td>
                <td>
                    <button type="button" class="btn btn-secondary" asp-controller="Vehicle" asp-action="Details" asp-route-id="@vehicle.Id">Details</button>
                </td>
                <td>
                    <button type="button" class="btn btn-secondary" asp-controller="Member" asp-action="Edit" asp-route-id="@vehicle.Id">Edit</button>
                </td>
                <td>
                    <button type="button" class="btn btn-danger" asp-controller="Member" asp-action="Delete" asp-route-id="@vehicle.Id">Delete</button>
                </td>
            </tr>
        }
    </tbody>
</table>